"""
Web MCP Server

This package provides an MCP server for agentic web access. ``fetch`` supports ordinary calls and
durable MCP tasks with progress and cancellation; ``search`` keeps its ordinary
synchronous contract; and ``fetch_events`` exposes full conversion history.

It is the seed of CLIO's web tooling and the test instrument for the sandbox
campaign's egress recording.
"""

__version__ = "2.1.2"
